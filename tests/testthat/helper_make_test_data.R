### ============================================================================
### fst files used in tests
###

# TODO: Should be old.fst and new.fst; stick in appropriate subdir (see Hadley's 
#		book). However, can't create old.fst without installing old version of 
#		fst package.
